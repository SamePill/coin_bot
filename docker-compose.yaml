version: '3.8'

services:
  aegis_bot:
    build: .
    container_name: aegis_bot_${ACCOUNT_ID}
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/app
    # DB 서비스가 docker-compose에 함께 있다면 주석 해제
    # depends_on:
    #   - db
    network_mode: "host"
